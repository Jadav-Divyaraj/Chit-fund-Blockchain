#include <iostream>  // Header file library for input and output
using namespace std; // Use standard library names without prefixes

int main() {         // The starting point where execution begins
    cout << "Hello World!"; // Print text to the console
    return 0;        // Signal that the program ended successfully
}
